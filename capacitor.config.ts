import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App API Star Wars',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
